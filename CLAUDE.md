# Claude 작업 규칙

## 응답 형식
- 모든 응답의 **마지막 줄**에 이번 턴에서 수정·생성·삭제한 파일 목록을 명시한다.
- 형식: `📝 수정: <relative path>, <relative path> · 생성: <path> · 삭제: <path>` (해당 항목만 표기)
- 코드 변경이 전혀 없는 턴(질문 답변·분석만)에는 `📝 수정 없음` 으로 표기한다.

## 작업 정책 (메모리 동기화)
- 기본 브랜치는 `main`. feature 브랜치는 명시 요청이 있을 때만.
- 워크트리 격리 사용 X — `main` 에서 직접 작업·커밋.
- 작업 완료 시 별도 확인 없이 git 커밋.
